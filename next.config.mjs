/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "towruyysurqliieeydud.supabase.co",
      },
    ],
  },
};

export default nextConfig;